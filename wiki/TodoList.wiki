#summary Here is the main TODO list for this project.

= TODO =

  # Add support for NITE functionality  
  # Doxygenate the code
  # Test the binding under Python 3.x
  # Route the cout/cerr streams to PyOut, so that OpenNI logs will be visible from the Python console
  # Modify the sample code to use the Python unittest framework
  # Make the Python code self-documenting by adding doc-strings to the binding
  # Add an XCode project for building the library under Mac OS X and update the installation instructions accordingly.
  # Add a makefile for building the library under Linux and update the installation instructions accordingly.